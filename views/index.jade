extends layout

block scripts
  != css('console')
  != js('console')

block content
  nav
    include _browserid
  .chat-container
    .chat
      .message
        span.nick tim
        span.text my message
      .command
        span.prompt
         = user ? user.username : 'guest'
         | @websh:~&gt;
        input(autocomplete='off')
