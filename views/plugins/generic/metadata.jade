if meta && typeof meta == 'object' && Object.keys(meta).length > 0
  h3 Details

  table.table.meta
    tbody
      for val, key in meta
        tr
          th 
            = key
          td
            if typeof val == 'array'
              = val.join(', ')
            else
              = val

